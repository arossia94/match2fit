import numpy as np
from utils import inspect_model

MODEL_SPECS = dict(id="test_47", collection="Granada", mass=1, pto="NLO", eft="NHO" )


def inv1(results):
	lamT1f3 = results.lamT1f3
	match2fit`Private`\[Alpha]$10465[1] = results.match2fit`Private`\[Alpha]$10465[1]
	return -0.25*(lamT1f3**2*match2fitxPrivatex\[Alpha]$10465(1))

def inv2(results):
	lamT1f3 = results.lamT1f3
	return List(np.abs(lamT1f3))

